bo-movies:
  display-name: ${spring.application.name}

info:
  name: ${spring.application.name}
#  version: 1.0

server:
  port: 7901

spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: ${spring.application.name}
